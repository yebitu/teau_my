<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ShopDAO">	


<!-- 	<resultMap type="shop" id="shopList">
		<id property="shopbId" column="SHOP_ID"/>
		<result property="orderCate" column="ORDER_CATE"/>
		<result property="" column=""/>
	</resultMap> -->
   
   
   <!-- 상품 추가 -->
   <insert id="insertShop">
   <![CDATA[
    INSERT INTO TEA(TEA_ID, ORDER_CATE, TEA_NAME, TEA_PRICE, TEA_IMG, TEA_INFO, TAG_BASE, TAG_CAFF, TAG_SEASON, TAG_USER, TAG_TASTE, TAG_EFF, TAG_BLEND, TAG_DRINK)
    VALUES((SELECT NVL(MAX(TEA_ID), 0) + 1 FROM TEA), #{orderCate}, #{teaName}, #{teaPrice}, #{teaInfo}, #{tagBase}, #{tagCaff}, #{tagSeason}, #{tagUser}, #{tagEff}, #{tagBlend}, #{tagDrink})
   ]]>
   </insert>
   
   <!-- 상품 업데이트 -->
   <update id="updateShop">
   <![CDATA[
   UPDATE TEA
   SET ORDER_CATE = #{orderCate}, TEA_NAME = #{teaName}, TEA_PRICE = #{teaPrice}, TEA_IMG = #{teaImg}, TEA_INFO = #{teaInfo}, TAG_BASE = #{tagBase}, TAG_CAFF = #{tagCaff}, TAG_SEASON = #{tagSeason}, TAG_USER = #{tagUser}, TAG_TASTE = #{tagTaste}, TAG_EFF = #{tagEff}, TAG_BLEND = #{tagBlend}, TAG_DRINK = #{tagDrink}
   WHERE TEA_ID = #{teaId}
   ]]>
   </update>
   
   
   <!-- 상품 삭제  -->
   <delete id="deleteShop">
   <![CDATA[
   DELETE TEA
   WHERE TEA_ID = #{teaId}
   ]]>
   </delete>
   
   <!-- 상품 상세 보기  -->
   <select id="getShop" resultType="shop">
   <![CDATA[
   SELECT * FROM TEA
   WHERE TEA_ID = #{teaId}
   ]]>
   </select>
 
	 <!-- 상품 목록 -->  
	 <select id="getShopList" resultType="shop">
	 <![CDATA[
		 SELECT * FROM TEA
	 	WHERE 1 = 1
	 ]]>
	 </select>

			
</mapper>
